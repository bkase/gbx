[package]
name = "testdata"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
once_cell = "1"
sha2 = "0.10"
hex = "0.4"

[build-dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
sha2 = "0.10"
hex = "0.4"
